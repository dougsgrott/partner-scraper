# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Raw fetch archive (large; re-fetchable but slow — keep it, never commit it)
raw/

# Local corpus + index (rebuildable; not committed)
data/*
!data/.gitkeep
state/*
!state/.gitkeep

# Generated change-feed reports (rendered from state/changes.db; not committed)
reports/
.claude/
CLAUDE.md

# Secrets
.env

# Tool caches
.ruff_cache/
